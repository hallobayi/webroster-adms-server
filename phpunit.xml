<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_DRIVER" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <!--
            The schema has `attendances.idoficina -> oficinas.idoficina`, but
            `oficinas.idoficina` is only indexed, not unique. MySQL accepts a foreign
            key to a non-unique indexed column; SQLite does not, and reports
            "foreign key mismatch" on insert. Laravel 12 now actually applies that
            constraint on SQLite, which broke the attendance upload test.

            The production database is MySQL, so the schema is left untouched and
            the SQLite test connection is kept permissive instead. See the note in
            README.md before tightening this.
        -->
        <env name="DB_FOREIGN_KEYS" value="false"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
    </php>
</phpunit>
